There is a huge issue that we do not auto-format or lint as we develop

## Prettier
we can push a single change to format everything and ignore that commit so we retain git history

##Eslint
self-explanatory